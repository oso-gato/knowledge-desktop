# knowledge-desktop — BUILD PLAN (ordered work packages; each = one PR)

Ordering principles: governance installed at the first code PR (N6/H4); empirical unknowns (C4/A11 concurrency, guacd interop, GPU plumbing) de-risked before deep investment; earliest end-to-end walking skeleton per lineage; L1 runtime work is host-gate-tiered from the start (its PID-1 image can't boot in the dev box's nested engine). Tier key: **in-box** (Tier-1 nested engine) / **host-gate** (`live-validate` label, fenced candidate incl. headscale) / **rehearsal**.

| ID | Title | Scope | FRs advanced | Tier | Deps |
|---|---|---|---|---|---|
| WP-00 | Control plane: repo governance | `require-PR` ruleset on `main`; poller enrollment of knowledge-desktop on the dev box; branch/label conventions; E6 disclosure-register doc skeleton; SECRETS.md schema v1 | H4, N6, E6 | control-plane (standalone PR) | — |
| WP-01 | CI skeleton + repo layout | Two-lineage tree (`shared/`, `lineage-xrdp/`, `lineage-grd/`, `gate/`, `deploy/`); build.yml matrix (PR build-only, main publish, weekly cron); digest-pinned Fedora bases; manifest resolve-log artifacts; static lints (quadlet parse, F4 disjointness, `.live-gate`↔quadlet drift incl. Secret line); probe-image build (supported-Fedora digest pin) | H4, §6.1, F3, F4 | in-box (CI) | WP-00 |
| WP-02 (PR1) | `.live-gate` v1 — strict E2 + real-login gate — **BUILT** (MVP of a vetted 7-PR arc, ZERO harness changes — rides existing SECRET_ENV/SECRET_MOUNT/MEMORY/PIDS keys). STRICT E2: skeleton-mode branches DELETED (entrypoints run kd-provision unconditionally `\|\| exit $?`), kd-health guard dropped, E6 R18 retired→narrow grd successor. Gate boots a REAL PROVISIONED box — roster-mixed.json inlined via SECRET_ENV_xrdp/web → /run/secrets/kd-roster; FENCE_xrdp gains shadow-utils/session caps + 4g/2048/30×6. PROBE_xrdp = quote-free census (kadm/gw1/gw2=2000/2001/2002; 5 D6 exclusions absent) + a REAL VNC login round-trip via new stdlib gate/probe/kd-vnc-login (RFB VncAuth **DES**, proven vs Xvnc) + kd-vnc-check (A13/V8: good+first-8 ACCEPT, cross-user+garbage rc-exact REJECT). PROBE_web = 3-tile census. gate/lint.sh: inline-roster≡fixture JSON-equality (negative-tested). **In-box PROVEN**: strict-E2 fail-fast (L2 rosterless→exit 3), the VNC DES matrix, both builds, lint. Positive real-roster boot→health+census+login = host-gate. DEFERRED PRs 2-7: EXPECT_EXIT harness verb + negsec/adminonly/badadmin siblings, COJOIN + xfreerdp probe container, fenced headscale + tailnet | H4, §6.2, E2 | in-box (E2/probes/lint) + host-gate (real login) | WP-01 |
| WP-02 (PR2-7) | gate harness completion (fedora-bootstrap EXPECT_EXIT verb; kd multi-run sibling targets; COJOIN co-boot; probe container; fenced headscale + tailnet handshake) | per the vetted plan (memory: wp02-gate-harness-plan) | H4, §6.2, §7, E2 | control-plane + host-gate | WP-02-PR1 |
| WP-03 | Deploy contract | `deploy/kd-{xrdp,grd}.container` (Secret=kd-roster, AddDevice `-/dev/dri` + `/dev/fuse`, GIDMap render-gid, three volumes nosuid,nodev, Notify=healthy, health keys, HealthOnFailure=kill, AutoUpdate, dedicated network); `setup.sh` full assertion set (subuid ≥2M, userns, crun, linger, sysctl, SELinux, render prereq); `spin-up.sh` wizard; rotation runbook | F1, E1, E2, B3-prereqs, B4-ops, F4 | in-box (lint) + host-gate | WP-01 |
| WP-04 | De-risk spike A (L1 concurrency) — **V1 GO recorded** (gate/spikes/FINDINGS.md §V1; host-gate spike, PR #27): grd RDP **login works** [NLA auth — closes WP-08's deferred login round-trip] AND grd **accepts a concurrent 2nd connection** [both reached channel-load; no single-session refusal — DESIGN [ADJ-25] hypothesis CONFIRMED] ⇒ L1 web=RDP has a stock path, **NOT a §10 fork**. Session STABILITY under the throwaway xfreerdp-3.29 test client is entangled with test-only artifacts [Fedora FreeRDP-3.29 experimental AV1 GFX; client cliprdr-FUSE denied by the fence; sw-render] the production guacd/FreeRDP-3.14 client does not share; the faithful stock-client stability + concurrency (+V2 Mutter-mirror/Stop-detach) proof rides the **COJOIN tier** [fedora-bootstrap, as WP-11-L2 already defers]. Reusable fact: the host-gate fence has NO runtime egress (bake probe tools at build time). Design input → WP-11-L1 pins conservative guac RDP params (GFX/AV1 + redirection off) | A11, C4, decides web-L1 transport → RDP | host-gate (spike done) | WP-02 |
| WP-05 | **De-risk spike B (L2 + gateway interop)** | V3 xorgxrdp external RandR acceptance; V4 guacd+pinned-FreeRDP pair CI build, guacd(GFX)→GRD paint, guacd(VNC)→x0vncserver paint; V5 L1 input-attribution spike | C4, A1, A11 | in-box (build) + host-gate (paint) | WP-02 |
| WP-06 | kd-provision core — **CORE SHIPPED** (provisioner + per-lineage `kd-cred` + roster fixtures + in-box suite `shared/tests/`; images carry it INERT — boot-chain invocation + the E2 fail-fast-on-absent-secret observable land WP-07/WP-08, host-gate roster probes ride WP-02) | Roster validation (extended schema), uidmap, accounts, two-pass credential derivation (stdin discipline), disable flow (per-lineage revoke verbs), D4 shared folder, grants files 0640, exit-force fail-fast, apply-failure policy + fixtures | D1–D6, G3–G5, A13, E1–E3 | in-box + host-gate (roster probes) | WP-03 |
| WP-07 | L2 walking skeleton — **SHIPPED** (Xorg/xorgxrdp-glamor/XFCE image; boot chain sesman→xrdp-loopback→provision→prestart-via-fd; startwm-kd user bus at $XDG_RUNTIME_DIR/bus; kd-health v1; .live-gate xrdp v1; in-box build+assembly+boot→kd-health suite (shared/tests/kd-l1-skeleton.test.sh — the full RDP round-trip/paint is HOST-GATE tier, the nested engine can't run isolated X). DEFERRED, named: tailscaled+async tailnet bind → WP-02-adjacent (needs the fence to prove); openh264 → V31 RFX fallback holds; roster-ABSENT strict E2 → WP-02 fixtures, E6 R18) | Fedora image: Xorg + xorgxrdp-glamor + XFCE core + notifyd; sesman prestart via provisioner fd; xrdp loopback+async-tailnet bind; user-bus session wiring; DPMS-off; kd-health; entrypoint. Gate: boots, RDP round-trip, health truthful, surface-enum | B1, A6, A12, C1, C3, F2 | in-box iterate → host-gate | WP-05, WP-06 |
| WP-08 | L1 walking skeleton — **BUILT (host-gate pending)** (the REAL headless-GNOME image, enabled by the host's new full-PID-1 e2e gate: per-user lingering `systemd --user` managers each running `gnome-shell --headless` [Wayland, llvmpipe software EGL — no gnome-session, no Xwayland, no Vulkan: `GSK_RENDERER=gl`; DESIGN §2 reconcile notes] + the SHIPPED `gnome-remote-desktop-headless` unit wired via a drop-in [compositor+pipewire deps, sw-EGL env, default.target install] serving RDP per user on a uid-derived FIXED port [RDP 3389+off — the [ADJ-34] L2 arithmetic, `negotiate-port=false`; grd's native VNC is DISABLED (the amendment: Fedora's grd headless-VNC offers only an anon-TLS security type no stock client speaks — DESIGN §2 note (j); 00-REQUIREMENTS A7 is now Lineage-2-only; L1 native-desktop access is RDP + the web door)]. Boot chain: `kd-provision.service` ENABLED [strict E2 exit-force, V19; `After=dbus-broker+logind` so pass-2 linger works] → pass-1 `kd-cred` [chpasswd + grdctl-stdin RDP full-phrase into the GKeyFile store — credential-only, no bus needed; V17 contingency named] → pass-2 `kd-session-enable` [linger → user bus → pipewire/wireplumber → per-user TLS cert → grdctl tls/port/enable ordered so the RDP listener actually binds → compositor+grd up]. L1 `kd-health` [systemd fully `running` + zero failed + provision-complete + EVERY user's manager + the RDP door answering + sshd]. A8 door [sshd.service enabled + the L2-proven key-only conf + tmux/resident-agent profile] + the FULL shared toolset [GNOME dconf Ptyxis default, V24] + agents env + nested-podman pkgs. `.live-gate` grd target GROWN to a provisioned-boot contract [roster SECRET_ENV, fence + label=disable + NET_BIND_SERVICE/SYS_CHROOT, HEALTH=kd-health, PROBE = census + 3 RDP listeners (3389/3390/3391) + assert the VNC port (5900) is SHUT — native VNC dropped, 240s budget]; quadlet HealthCmd/StartPeriod/caps updated in lockstep [drift lint]. **In-box PROVEN**: build + `kd-l2-skeleton.test.sh` assembly asserts [stack present, units enabled, dconf compiled, grd-VNC-disabled + no VNC probe shipped, no L2 leaks]. ALL runtime proof = the host PID-1 gate [this PR's live-validate]. DEFERRED honestly: V1 2nd-RDP + V2 Mutter-mirror [WP-04 spike probes, gate-keeper for WP-11/12-L1]; RDP-login round-trip probe [no stdlib RDP+CredSSP client; the RDP listener + credentials.ini prove the credential path]; vault L1 timer [WP-17-L1]; `kd-web-grd` sidecar [N6 standalone] | B1, A6, A12, C1, C3, F2 | in-box (build+assembly) + host-gate (ALL runtime) | WP-04, WP-06 |
| WP-09 (L2) | VNC door — L2 — **BUILT** (in-session `doors-agent` starts `x0vncserver` on the session Xorg: loopback `-localhost`, rfbport 5900+N, `-AlwaysShared -AcceptSetDesktopSize`, VncAuth ← `~/.kd/vncpasswd` [the A13 first-8 kd-cred already installs]; kd-health VNC tier [provisioned boots: x0vncserver alive + a loopback RFB port answers; skeleton mode skips]; `.live-gate` PROBE enumerates VNC ports; pkg `tigervnc-x11-server`, DESIGN §17 name corrected. DEFERRED honestly: tailnet native-VNC listener → tailnet WP; cross-container web-tile reach → WP-11; live RFB round-trip + V8 truncation → host-gate w/ roster [WP-02] — the nested engine can't run isolated X, skeleton gate has no session) | L2 per-user x0vncserver (loopback + tailnet listeners, `~/.kd/vncpasswd`); truncation probes (V8) | A7, A13, A12 | in-box build/assembly + host-gate | WP-07 |
| WP-09 (L1) | VNC door — L1 — **VOID** (native VNC dropped on the GRD primary lineage — the amendment: Fedora's grd headless-VNC offers only an anon-TLS RFB security type no stock VNC client speaks, and stock-package provenance forbids a custom grd. L1 native-desktop access is RDP (A6) + the web door (A1); 00-REQUIREMENTS A7 is now Lineage-2-only. See DESIGN §2 note (j)) | — | — (A7 met on L2 only) | — | — |
| WP-10 | Web door core — **BUILT + HOST-GATE GREEN** (two-stage image: guacd 1.6.0 + vendored FreeRDP 3.14 [RDP/VNC/SSH plugins, ldd-verified] → runtime Caddy :443 / Tomcat 9.0.120 / guacamole.war 1.6.0 + jdbc·totp·ban / postgres 127.0.0.1; all artifacts sha-verified. DB pw generated at boot, none in image [E1]; kd-web-sync TOTP-preserving disable [D5]; Tomcat 127.0.0.1-bind + RemoteIpValve valid-inside-`</Host>` internalProxies=127.0.0.1 [A2]; ban default-512 not-0 [A4]; guacadmin disabled + auto-create off [A12]; truthful deep-liveness kd-health [N5]; Caddy `tls internal` on a NAMED loopback site + `skip_install_trust` [gate posture; ACME/domain = deploy-time rewrite]; `.live-gate` `web` target with minimal `FENCE_web` cap add-backs [SETUID/SETGID/SETPCAP/CHOWN/NET_BIND_SERVICE — DAC_OVERRIDE deliberately absent: userns-ineffective, so every file write runs AS its owner]. **Host-gate GREEN** after 4 converging cap-floor/integration fixes, each reproduced + validated in-box. DEFERRED: kd-provision→kd-web-sync boot wiring + python3 → WP-11; ACME/roster site rewrite → deploy WP; A3/A4/A11 live round-trip = manual/browser tier) | Caddy + Tomcat 9 + guacamole.war + jdbc/totp/ban + postgres 127.0.0.1; guacd+FreeRDP pair from WP-05; kd-web-sync (TOTP-preserving disable); pinned RemoteIpValve; deep-liveness health; TLS internal (gate) / ACME (prod) | A1–A5, A12, A13, D5, F2 | in-box build + host-gate | WP-05, WP-06 |
| WP-11 (L2) | Web desktop tile — L2 — **BUILT** (kd-web-sync `apply-user <user> <uid>` upserts the per-user guac VNC connection row: 127.0.0.1:(5900+uid−2000) [the [ADJ-34] in-netns loopback endpoint], first-8 password param [at rest: E6 R7], color-depth pinned 32, READ grant to that user's entity ONLY [A9/A12]; kd-provision passes the uid [an identity, not a secret]; doors-agent port derivation switched display→uid [STABLE across session reorder — kd-provision's append-only uidmap, E3]; kd-web-sync peer-auths over the unix socket as kdweb [the old TCP/scram default could never have authenticated — fixed]; web entrypoint runs kd-provision at boot [python3+acl added; roster-absent ⇒ skeleton mode, E6 R18] + persists the DB credential INSIDE the pg volume [recreation-survival E3 fix]. **In-box PROVEN against a live guac schema**: tile rows + verifier-scheme match + idempotent re-apply + D5 disable preserves TOTP+tile + uid guards + full roster→kd-provision→kd-web-sync→rows e2e. DEFERRED: tri-path marker-a11/paint proof → the COJOIN gate-harness extension [fedora-bootstrap, control-plane]; L1 rows → WP-04 V1 GO + WP-08) | L2 VNC connection rows (derived secret storage + E6 line); marker-a11 web leg; guacd rows pinned 32bpp | A11, A9, C4-web | in-box (SQL/e2e) + host-gate (paint, post-COJOIN) | WP-09, WP-10 |
| WP-11 (L1) | Web desktop tile — L1 — **UNBLOCKED** (WP-04 V1 GO recorded + WP-08 merged): L1 per-user guacd **RDP** rows → grd `3389+off` with `${GUAC_USERNAME}`/`${GUAC_PASSWORD}` token passthrough [full phrase, A12 re-resolution] + **conservative params** [GFX/AV1 off, no clipboard/drive redirection — WP-04 stability input] + READ grant to that user's entity only [A9/A12]; in-box proof vs a live guac schema [like WP-11-L2]. Paint/marker-a11 + session stability = COJOIN tier | A11, A9, C4-web | in-box (SQL/e2e) + host-gate (paint+stability, post-COJOIN) | WP-04 GO, WP-08 |
| WP-12 | Geometry (C4) — **L1 GOVERNOR BUILT (MVP core); gate-keepers RECORDED** (gate/spikes/FINDINGS.md: **V2 GO** — Mutter `ApplyMonitorsConfig` applies an external config on grd [`apply=APPLIED-OK ascale=1.0→1.25`]; **V3 NO-GO** — L2/xorgxrdp rejects external `RRSetScreenSize` [`BadMatch`], so C4-concurrent on L2 degrades to last-client-resize — a §5 lineage delta + E6 residual, agent-decided pending owner ratification). **L1 geometry-governor** (`lineage-grd/geometry-governor`, python3-gi): on a monitors/governance change it MIRRORS every attached grd virtual monitor onto ONE clone logical monitor at the GOVERNING display's resolution via `ApplyMonitorsConfig` [the V2-proven mechanism]; governance = `$XDG_RUNTIME_DIR/kd-geometry-governing` [written by the input-attribution component] else the sole/primary monitor [so single/sequential C4 works with no attribution]; a monitor lacking the governing mode is DROPPED [Mutter clone same-mode constraint, disclosed]; debounced re-assert + no-storm guard + 3s poll backstop [≤5s C4 budget]. `--global`-enabled per-user unit (`kd-geometry-governor.service`) + explicit start in `kd-session-enable` [C3]; `python3-gobject` now a permanent grd dep. **In-box PROVEN** [`shared/tests/kd-geometry-governor.test.sh`]: the PURE mirror-config computation — clone at the governing resolution, same-mode DROP disclosed, governance selects the resolution, default-to-first, no-storm — 6 fixture cases GREEN. DEFERRED (WP-12 follow-ups): the input-attribution shell component [libei per-connection device, V5]; the L2 geometry-arbiter [XInput2 attribution + last-resizer accept, per V3]; the geometry oracle `kd-geometry` [BUILT — X11 xrandr + Wayland Mutter parsers, unit-tested `shared/tests/kd-geometry.test.sh`; on PATH both lineages; xrandr now a permanent xrdp dep]; the other three ops CLIs [kd-sessions/-marker/-render-report]; the full C4 matrix runtime proof [≤5s follow, single-click transfer, detach, apps-undisturbed across 2 concurrent connections] = COJOIN tier | C4, A11-concurrent, C2 | in-box (governor unit-test + build/assembly) + host-gate/COJOIN (C4 matrix) | WP-11, V2 GO |
| WP-13 (L2) | SSH/Mosh/tmux terminal door (A8) — L2 — **BUILT** (openssh-server key-only + mosh + tmux; sshd_config.d/10-kd.conf = PasswordAuthentication no / PubkeyAuthentication yes / PermitRootLogin no / no forwarding [D3 least-priv] / ListenAddress 127.0.0.1 [tailnet ListenAddress rides the tailnet WP; A2 by no-siblings-net meanwhile]; the one persistent per-user tmux session "kd" via /etc/profile.d/kd-tmux.sh — PROFILE-based not ForceCommand so it works for ssh AND mosh [ForceCommand pre-empts mosh-server]; entrypoint generates host keys + starts+supervises sshd; kd-health SSH tier [sshd alive + loopback :22 answers]; authorized_keys are kd-provision's [WP-06]. **In-box PROVEN end-to-end** [sshd needs no X]: a real key logs in → lands in tmux session kd, password auth REFUSED, root REFUSED; session persistence across reconnects holds BY MECHANISM, not a named reconnect test [A8 one-per-user: `tmux -A` re-attaches the one named session, and no logind exists on this login path to reap it]. DEFERRED: the resident-agent lifecycle [G1/G2] is systemd-natural → WP-08/L1 + a per-user L2 mechanism; the tailnet ListenAddress + gate ssh-door login probe need real fixture keys [WP-02 plan deferral]. L1 SSH door rides WP-08) | openssh key-only + mosh + tmux one-session-per-user; sshd-up health | A8, A12, D3 | in-box (key-login/tmux/refusals) + host-gate | WP-06 |
| WP-13 (agents) | resident agents (G1/G2) — **BUILT** (two components). **(1) vcs-env keystone** (#22-sibling): per-user git identity + GitHub credential via `shared/kd-agent-env`, run AS the user by kd-provision gated on the roster `vcs` block — git `user.name`/`user.email`, `credential.helper store` + `~/.git-credentials` [0600 store backend], and `~/.config/kd/vcs.env` [0600, `GH_TOKEN`/`GITHUB_TOKEN`]. TOKEN rides STDIN only [E1 — never argv/log/other-uid env]; written with bash builtins so nothing lands in `/proc/*/cmdline`; owner-uid residual disclosed [E6]. `git-core` + `gh` [class-(a)] named explicitly [N2]. The credential BOTH the resident agent's GitHub ops AND the E4 vault's production private-GitHub clone [WP-17] need. Proven: 15-case engine-free suite [`shared/tests/kd-agent-env.test.sh`, incl. **E1 token-absent**] + container integration [per-user credential ISOLATION, no token in provisioner output, gh installed]. **(2) resident window** (merged #22): `kd-tmux.sh` seeds the persistent "kd" session on first login with an "agent" window [`kd-agent-run` → sources `vcs.env`, runs Claude Code in `$HOME`, exits to `/bin/bash -l` so the window/A8 session never dies] + a "shell" window, landing on the agent [lives in the tmux SERVER → survives disconnects, A8]. Proven: 5-case engine-free [`shared/tests/kd-agent-run.test.sh`] + real-tmux container test [agent+shell windows, lands on agent, stays alive]. DEFERRED: `kd-agent-refresh` currency [F3, L2 driver/L1 timer]; that Claude STAYS resident + live `gh auth` [host-gate, real token]; H3 PR-only) | G1, G2, H3 | in-box (identity/credential/E1 + session structure) + host-gate (agent runtime, +R for G2 egress) | WP-08, WP-15 |
| WP-14 | Fleet tiles + grants — **BUILT** (kd-web-sync `apply-user` now also upserts one guac **SSH connection per GRANTED endpoint** [A10]: kd-provision's per-user grants file [`<tile> <host> <port> <user>`, 0640 root:kd-door, exact-match fail-closed, regenerated each run] is ferried to the kdweb half via `KD_TILES` [tile lines are IDENTITIES not secrets — E6 R19-class]; the OPTIONAL per-user **tile private key** [roster `tile_ssh_key`] rides the SAME stdin AFTER the phrase line [E1: secret, never argv/env] → a `private-key` connection param [key-less tiles still row: guac prompts interactively, disclosed]. Rows are name-keyed `<user>-tile-<tile>`, **READ-granted to that user's entity ONLY** [A9/A10], and **REVOKED by regeneration** [D5]: a shrunk/emptied grants set DELETES exactly that user's now-ungranted tiles — double-scoped by name-key AND own-entity-permission so the desktop tile and every other user's rows are untouchable. kd-provision reordered [grants BEFORE apply] so first-boot tile sync is non-empty. **In-box PROVEN** [`shared/tests/kd-fleet-tiles.test.sh` — the WP-11 schema-real tier]: 3 SSH tiles from the roster grants, host/port/user correct, per-user key present/absent, per-entity isolation, idempotent re-apply, partial + full revocation clean [neither the desktop tile nor another user's tiles collateral-deleted]. DEFERRED honestly: the live web LOGIN through a tile to an in-netns sshd stand-in [V11 tri-path, host-gate/manual]) | A10, D5 | in-box (schema e2e) + host-gate (live tile login) | WP-10, WP-06 |
| WP-15 (L2) | Toolset (B2) — L2 — **BUILT** (reusable `shared/toolset/`: vendor-repos.sh writes gpgcheck=1 repos [VS Code=Microsoft, 1Password=vendor, Claude=Anthropic `latest`] + imports keys; dnf installs firefox·ptyxis·code·1password·1password-cli·claude-code; Obsidian 1.12.7 class-c tar.gz sha256-pinned fail-closed [`fcbe08b1…`] → /opt/obsidian. Ptyxis default via exo helpers.rc [L2-#10, not xfconf]; Claude DISABLE_UPDATES+DISABLE_AUTOUPDATER managed-settings [ADJ-16 — no home shadow]; obsidian.desktop launcher. **In-box PROVEN**: build fail-closed on every binary + the Obsidian hash; provenance verified [Microsoft/1Password/Anthropic PBC/Fedora]; no /root claude shadow; ALL 3 Electron sandboxes setuid 4755 [V23 build-anchored — Obsidian's shipped 0755 corrected]; DISABLE_UPDATES + Ptyxis-default asserted. DEFERRED to host-gate/WP-02 session tier: the RUNTIME probes [apps launch in-session V24, Electron starts sans --no-sandbox V23, idle-blank, claude adoption V30] — the skeleton gate has no session; L1 half rides WP-08) | Obsidian pin (sha256 fail-closed) · VS Code · Firefox · 1Password · Ptyxis default · Claude Code (`DISABLE_UPDATES=1`); shadow-binary + Electron-sandbox + idle-blank probes | B2, F3 | in-box build/assembly + host-gate (launch probes) | WP-07 |
| WP-15 (L1) | Toolset (B2) — L1 (rides WP-08: reuse `shared/toolset/`, Ptyxis default via GNOME mechanism) | same toolset, L1 lineage; per-lineage Ptyxis-default mechanism | B2, F3 | host-gate | WP-08 |
| WP-15b | Control plane: tile pubkeys cross-repo | PRs to the fleet host repos (Erebus setup / fedora-bootstrap; fedora-dev) consuming per-user tile public keys | A10 (rehearsal precondition) | control-plane (other repos) | WP-14 |
| WP-16 | Nested podman (B3) — **BUILT (MVP core)** (each provisioned user gets working rootless podman: `kd-provision` `setup_nested_podman` writes a per-user `/etc/subuid`+`/etc/subgid` band `200000 + (uid−2000)×65536` size 65536 [idempotent read-modify-write preserving other users' + system lines; non-overlapping by construction] + a per-user `~/.config/containers/storage.conf` [driver=overlay, graphroot on the /home volume, `mount_program=/usr/bin/fuse-overlayfs` PINNED for determinism across host classes + the gate, [ADJ-12]], chowned to the user; lineage-xrdp base gains `podman crun passt fuse-overlayfs` [shadow-utils already provides newuidmap/newgidmap]. **In-box PROVEN engine-free**: band math kadm=200000/gw1=265536/gw2=331072 non-overlapping, D6-excluded users get no band, idempotent re-apply keeps foreign lines, storage.conf fuse-overlayfs+graphroot+owner correct; L2 image builds with the packages. DEFERRED honestly to host-gate/later: a REAL nested `podman build`+run as an unprivileged user [the nested-build-b3 RUN probe — the degraded dev engine can't nested-nested run, HOST-GATE tier per DESIGN §7]; baked seed/ingest OCI archives + per-user `podman load` [rides WP-18 ingest]; N=5 concurrency sizing [rehearsal/host tier]) | B3 | host-gate | WP-06, WP-03 |
| WP-17 | Vault sync (E4) — **BUILT (MVP core)** (lineage-agnostic `kd-vault-sync` guarded cycle: stop-flag + `vault-ready` gates → guarded commit [local bulk-delete bound `max(25,10%)`] → guarded fetch/merge [three-dot merge-base inbound bound; conflict ⇒ `merge --abort` byte-identical restore + stop-flag + best-effort bus notification] → plain push; every step FAIL-SAFE. `kd-vault-init` [per-user clone via a sibling temp dir — a pre-existing non-clone `~/Vault` is NEVER deleted (clone withheld + alert, data preserved) + broadened `.gitignore` seed + self-healing fail-safe remote-protection gate: local-bare `receive.deny*` / GitHub `/rules/branches/{branch}` effective rules → `~/.kd/vault-ready`; local-path bare trusted via scoped `safe.directory`]. `kd-vault-driver` = the L2 entrypoint-supervised 5-min sweep [no systemd `--user`; L1 timer rides WP-08], logging per-user sync output. kd-provision wires it per user gated on `box.vault_repo` [absent ⇒ E2 degrade]; `git-core` added. **In-box PROVEN**: 60-case engine-free suite [`shared/tests/kd-vault.test.sh` — every guard incl. mass-delete both directions + the F1 local-bulk-import false-positive regression, conflict restore, ff/divergent merge incl. with NO ambient git identity (the production runuser env — sync supplies its own on commit AND merge), ruleset confirmed/absent/unverified, F2 effective-rules parse, driver selection+fail-safe, F3 pre-existing-vault preservation] + b3-container integration [clone/owned/`.gitignore`/vault-ready + real per-user push round-trip]. DEFERRED honestly: the live `+R` round-trip + config-churn soak on a fenced remote [gate-harness fixture extension, fedora-bootstrap-side, like WP-02 PR2-7]) | E4 | host-gate (+R round-trip) | WP-13, WP-15 |
| WP-18 | Ingest sandbox (E5) | ingest CLI two-stage, containment probe set, pasta-reachable fixture, resource-bound enforcement test (or honest re-scope) | E5 | host-gate | WP-16 |
| WP-19 | Self-maintenance (F3) | Weekly cron live; auto-update wiring verified; Obsidian bump-PR CI job; adoption test (claude version == rpm NVR) | F3 | in-box (CI) + rehearsal (rollback drill) | WP-15 |
| WP-20 | Gate completion + hardening probes | **rdp-handshake — BUILT** (`gate/probe/kd-rdp-check`, stdlib X.224: a real TPKT/X.224 Connection-Request→Connection-Confirm round-trip REPLACES the bare `exec 3<>/dev/tcp` connect that a dumb listener passed identically — grd 3389/3390/3391 + xrdp 3389; rc-exact 0/1/2, self-diagnosing; NO credential exchanged, so PROTOCOL-only [A12/A13 RDP identity = deferred NLA tier], loopback-only [the tailnet-iface leg = host fenced-headscale harness, V7]; **in-box PROVEN** `shared/tests/kd-rdp-check.test.sh` — valid CC/NegRsp/NegFailure/plain accept, bare-listener + non-RDP-banner reject, closed-port conn-error; real grd/xrdp handshake = this PR's host-gate); lockout two-source, secrets-e1-scan + boot sampler, isolation-d2d3, bpp-fork, health drills incl. never-banned, E2 optional-degrade runs, cloud-sync template lint | C2, A4, E1, D2/D3, F2, E2 | host-gate | WP-10…WP-18 |
| WP-21 | Dress rehearsal — Erebus | REHEARSAL.md + rehearsal.sh; both lineages incl. co-deploy (F4), external scan, ACME, reboot, rollback drill, real clients, TOTP phone, tiles → Erebus + fedora-dev, capacity soak | §6.4, A2, A10, C2, C3, F3, F4 | rehearsal | WP-20, WP-15b |
| WP-22 | Dress rehearsal — Strix (B4) | Byte-identical quadlet; GPU evidence chain (per-user renderer, gpu_busy, fds); encode state recorded; full probe diff vs Erebus; GIDMap-vs-udev fallback decision recorded | B4 | rehearsal | WP-21 |

**Merge discipline:** every WP ships through PR + `live-validate` + fitness (the fleet's two independent machine gates, H4). WP-00/-02/-03/-15b are control-plane class and ship standalone (N6). WP-04/-05 are gate-keepers: WP-11/-12 do not start until their GO/NO-GO is recorded; a NO-GO triggers the conditional forks in DESIGN.md §11 instead of silent rework.
